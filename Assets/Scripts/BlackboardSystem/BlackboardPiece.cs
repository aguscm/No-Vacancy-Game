using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlackboardPiece : MonoBehaviour
{
    public Blackboard blackboard;
    public enum correctValue {Null, LeftEnd,MiddleLeft,Middle,MiddleRight,RightEnd};
    public correctValue correctvalue;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
